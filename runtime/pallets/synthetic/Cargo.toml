[package]
name = "pallet-synthetic"
version = "0.1.0"
edition = "2024"

[dependencies]
codec = { workspace = true }
polkadot-sdk = { workspace = true, features = ["runtime"] }
scale-info = { workspace = true }

[features]
default = ["std"]
std = [
    "codec/std",
    "polkadot-sdk/std",
    "scale-info/std",
]
